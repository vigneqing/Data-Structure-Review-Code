# 数据结构考研知识点总结

---

## 一、绪论

### 1.1 数据结构的基本概念
- **数据**：信息的载体，是描述客观事物的数、字符及所有能输入到计算机中被程序处理的符号的集合。
- **数据元素**：数据的基本单位，通常作为一个整体考虑。
- **数据项**：构成数据元素的不可分割的最小单位。
- **数据对象**：性质相同的数据元素的集合。
- **数据结构**：相互之间存在一种或多种特定关系的数据元素的集合。包括三要素：**逻辑结构**、**存储结构**、**数据运算**。

### 1.2 数据结构三要素

#### 逻辑结构（定义"关系"）
| 逻辑结构 | 特点 |
|---------|------|
| 集合 | 元素同属一个集合，无其他关系 |
| 线性结构 | 一对一关系（线性表、栈、队列、串） |
| 树形结构 | 一对多关系（树、二叉树） |
| 图状/网状结构 | 多对多关系（图） |

#### 存储结构（物理结构）
| 存储结构 | 特点 |
|---------|------|
| 顺序存储 | 逻辑相邻则物理相邻，用数组实现 |
| 链式存储 | 用指针链接，逻辑相邻物理不一定相邻 |
| 索引存储 | 建立索引表，通过索引找到地址 |
| 散列存储 | 通过散列函数计算存储地址 |

#### 数据运算
- 运算的定义（针对逻辑结构）
- 运算的实现（针对存储结构）

### 1.3 算法与算法分析

#### 算法五大特性
1. **有穷性**：有限步后结束
2. **确定性**：每条指令有确切含义
3. **可行性**：可通过基本运算执行
4. **输入**：零个或多个输入
5. **输出**：一个或多个输出

#### 时间复杂度
- **$O(1)$**：常数阶
- **$O(\log_2 n)$**：对数阶
- **$O(n)$**：线性阶
- **$O(n\log_2 n)$**：线性对数阶
- **$O(n^2)$**：平方阶
- **$O(n^3)$**：立方阶
- **$O(2^n)$**：指数阶
- **$O(n!)$**：阶乘阶

**计算规则**：
- 加法规则：$T(n) = T_1(n) + T_2(n) = O(\max(f(n), g(n)))$
- 乘法规则：$T(n) = T_1(n) \times T_2(n) = O(f(n) \times g(n))$

#### 空间复杂度
- 算法运行过程中所需的额外存储空间大小
- 若额外空间相对于输入数据量是常数，则为 **$O(1)$**（原地工作）

---

## 二、线性表

### 2.1 线性表的定义与基本操作
- **定义**：$n$ 个相同类型数据元素的有限序列，$n$ 为表长
- **特点**：第一个元素无前驱，最后一个元素无后继，其余元素有且仅有一个前驱和后继

**基本操作**：
| 操作 | 描述 |
|------|------|
| `InitList(&L)` | 初始化空表 |
| `DestroyList(&L)` | 销毁表 |
| `ListEmpty(L)` | 判空 |
| `ListLength(L)` | 求表长 |
| `LocateElem(L, e)` | 按值查找 |
| `GetElem(L, i, &e)` | 按位查找 |
| `ListInsert(&L, i, e)` | 插入 |
| `ListDelete(&L, i, &e)` | 删除 |
| `PriorElem(L, cur_e, &pre_e)` | 求前驱 |
| `NextElem(L, cur_e, &next_e)` | 求后继 |

### 2.2 顺序表（顺序存储）

```c
// 静态分配
#define MaxSize 10
typedef struct {
    ElemType data[MaxSize];
    int length;
} SqList;

// 动态分配
#define InitSize 10
typedef struct {
    ElemType *data;
    int MaxSize;
    int length;
} SqList;
```

**特点**：
- 可随机存取，时间复杂度 $O(1)$
- 存储密度高
- 插入、删除需移动大量元素，时间复杂度 $O(n)$
- 需要连续存储空间

**操作复杂度**：
| 操作 | 平均时间复杂度 |
|------|:---:|
| 按位查找 | $O(1)$ |
| 按值查找 | $O(n)$ |
| 插入 | $O(n)$ |
| 删除 | $O(n)$ |

### 2.3 单链表（链式存储）

```c
typedef struct LNode {
    ElemType data;
    struct LNode *next;
} LNode, *LinkList;
```

**特点**：
- 不支持随机存取，按位查找 $O(n)$
- 插入、删除不需要移动元素，$O(1)$（已知位置）
- 存储密度较低（有指针开销）

#### 头结点 vs 不带头结点
- **带头结点**：空表判断 `L->next == NULL`，操作统一
- **不带头结点**：空表判断 `L == NULL`，需特殊处理首元素

#### 链表操作关键点
- **头插法**（逆序建立链表）：新结点始终插入头结点之后
- **尾插法**（顺序建立链表）：需维护尾指针
- **逆置**：三指针法（pre, cur, next）

### 2.4 双链表

```c
typedef struct DNode {
    ElemType data;
    struct DNode *prior, *next;
} DNode, *DLinkList;
```

- 可双向遍历
- 插入/删除需同时修改 prior 和 next 指针

### 2.5 循环链表
- **循环单链表**：最后一个结点的 next 指向头结点
- **循环双链表**：头结点的 prior 指向尾结点

### 2.6 静态链表
```c
#define MaxSize 50
typedef struct {
    ElemType data;
    int next; // 游标，-1 表示结束
} SLinkList[MaxSize];
```

---

## 三、栈和队列

### 3.1 栈（Stack）

**定义**：只允许在一端（栈顶）进行插入和删除的线性表，**LIFO**（后进先出）。

#### 顺序栈
```c
#define MaxSize 10
typedef struct {
    ElemType data[MaxSize];
    int top; // 栈顶指针
} SqStack;
```
- **top 初始值**：通常 `-1`（表示空栈），入栈 `++top`，出栈 `top--`
- 或 `top = 0`，入栈 `data[top++] = x`，出栈 `x = data[--top]`
- **栈满**：`top == MaxSize - 1`
- **栈空**：`top == -1`

#### 共享栈
```c
typedef struct {
    ElemType data[MaxSize];
    int top0; // 0号栈栈顶，从 -1 开始增长
    int top1; // 1号栈栈顶，从 MaxSize 开始减少
} ShStack;
```
- **栈满**：`top0 + 1 == top1`

#### 链栈
```c
typedef struct LinkNode {
    ElemType data;
    struct LinkNode *next;
} *LiStack;
```
- 以链表头端作为栈顶，操作都在表头进行

#### 栈的应用
1. **括号匹配**
2. **表达式求值**（中缀转后缀、后缀计算）
3. **递归**（函数调用栈）
4. **迷宫求解**
5. **进制转换**

#### 表达式转换
| 表达式 | 运算符位置 | 示例 |
|--------|----------|------|
| 中缀表达式 | 运算符在操作数中间 | `a + b` |
| 前缀表达式 | 运算符在操作数前面 | `+ a b` |
| 后缀表达式 | 运算符在操作数后面 | `a b +` |

**中缀转后缀算法**（运算符优先级）：
- 操作数直接输出
- 左括号入栈
- 右括号：弹出栈内运算符直到遇到左括号
- 运算符：弹出所有优先级 >= 当前运算符的栈内运算符后再入栈

### 3.2 队列（Queue）

**定义**：只允许在一端（队尾）插入，另一端（队头）删除的线性表，**FIFO**（先进先出）。

#### 顺序队列（循环队列）
```c
#define MaxSize 10
typedef struct {
    ElemType data[MaxSize];
    int front, rear; // 队头、队尾指针
} SqQueue;
```

**三种判断队空/队满方式**：

| 方式 | 队空 | 队满 |
|------|------|------|
| 牺牲一个单元 | `front == rear` | `(rear + 1) % MaxSize == front` |
| 增加 size 成员 | `size == 0` | `size == MaxSize` |
| 增加 tag 标志 | `front == rear && tag == 0` | `front == rear && tag == 1` |

**关键公式**（牺牲一个单元法）：
- 入队：`rear = (rear + 1) % MaxSize`
- 出队：`front = (front + 1) % MaxSize`
- 队列长度：`(rear - front + MaxSize) % MaxSize`

#### 链队
```c
typedef struct LinkNode {
    ElemType data;
    struct LinkNode *next;
} LinkNode;

typedef struct {
    LinkNode *front, *rear;
} LinkQueue;
```
- 带头结点方便操作

#### 双端队列
- 允许两端都可以入队和出队

### 3.3 栈和队列的应用

#### 栈在括号匹配中的应用
- 遇到左括号入栈，遇到右括号出栈匹配

#### 栈在表达式求值中的应用
- 运算符栈 + 操作数栈，按优先级计算

#### 队列在层次遍历中的应用
- 二叉树层次遍历、图的广度优先搜索

#### 队列在计算机系统中的应用
- 打印队列、CPU 进程调度、缓冲区

---

## 四、串

### 4.1 串的定义与基本操作

```c
// 定长顺序存储
#define MaxLen 255
typedef struct {
    char ch[MaxLen];
    int length;
} SString;

// 堆分配存储
typedef struct {
    char *ch;
    int length;
} HString;
```

### 4.2 串的模式匹配

#### 简单模式匹配（BF 算法）
- 时间复杂度：最坏 $O(mn)$，最好 $O(n)$

#### KMP 算法
- 时间复杂度：$O(m + n)$
- **核心**：利用已匹配部分的信息，避免主串指针回溯
- **next 数组**：模式串失配时的回退位置

**next 数组手工求法**：
- `next[1] = 0`
- 看模式串第 $j$ 位前的子串，求最长相等前后缀长度 $k$，则 `next[j] = k + 1`
- 若没有相等前后缀，`next[j] = 1`

**nextval 数组**（优化）：
- 若 `P[j] == P[next[j]]`，则 `nextval[j] = nextval[next[j]]`
- 否则 `nextval[j] = next[j]`

---

## 五、树与二叉树

### 5.1 树的基本概念

#### 基本术语
| 术语 | 含义 |
|------|------|
| 结点的度 | 结点的孩子个数 |
| 树的度 | 树中结点的最大度数 |
| 叶子结点 | 度为 0 的结点 |
| 分支结点 | 度 > 0 的结点 |
| 路径长度 | 路径所经过的边数 |
| 结点的深度 | 从根结点到该结点经过的边数（自上而下） |
| 结点的高度 | 从叶结点到该结点经过的边数（自下而上） |
| 树的高度（深度） | 树中结点的最大层数 |

#### 树的性质
1. 树中的结点数 = 所有结点的度数之和 + 1
2. 度为 $m$ 的树中第 $i$ 层上至多有 $m^{i-1}$ 个结点
3. 高度为 $h$ 的 $m$ 叉树至多有 $\frac{m^h - 1}{m - 1}$ 个结点

### 5.2 二叉树

#### 定义
每个结点至多有两棵子树，且左右子树有顺序之分。

#### 二叉树的性质
| 性质 | 内容 |
|------|------|
| 性质1 | 第 $i$ 层至多有 $2^{i-1}$ 个结点 |
| 性质2 | 高度为 $k$ 的二叉树至多有 $2^k - 1$ 个结点 |
| 性质3 | $n_0 = n_2 + 1$（叶子数 = 度为2的结点数 + 1） |
| 性质4 | $n$ 个结点的完全二叉树高度为 $\lceil \log_2(n+1) \rceil$ 或 $\lfloor \log_2 n \rfloor + 1$ |
| 性质5 | 完全二叉树中编号为 $i$ 的结点：左孩子 $2i$，右孩子 $2i+1$，父结点 $\lfloor i/2 \rfloor$ |

#### 特殊二叉树
| 类型 | 特点 |
|------|------|
| 满二叉树 | 高度为 $h$，结点数为 $2^h - 1$ |
| 完全二叉树 | 对应满二叉树的编号完全一致（允许最后一层右边缺失） |
| 二叉排序树 | 左子树所有结点 < 根 < 右子树所有结点 |
| 平衡二叉树 | 任意结点左右子树高度差 ≤ 1 |

#### 二叉树的存储结构

```c
// 顺序存储（适合完全二叉树）
typedef ElemType SqBiTree[MaxSize];

// 链式存储
typedef struct BiTNode {
    ElemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;
```

#### 二叉树的遍历

| 遍历方式 | 顺序 | 应用 |
|----------|------|------|
| 先序遍历 | 根 → 左 → 右 | 前缀表达式 |
| 中序遍历 | 左 → 根 → 右 | 二叉排序树有序遍历 |
| 后序遍历 | 左 → 右 → 根 | 后缀表达式、树的高度计算 |
| 层次遍历 | 从上到下，从左到右 | BFS |

**遍历序列性质**：
- 已知 **先序 + 中序** 可唯一确定一棵二叉树 ✅
- 已知 **后序 + 中序** 可唯一确定一棵二叉树 ✅
- 已知 **先序 + 后序** 不能唯一确定 ❌（只能确定父子关系，不能确定左右）

```c
// 递归遍历模板（以中序为例）
void InOrder(BiTree T) {
    if (T != NULL) {
        InOrder(T->lchild);
        visit(T);
        InOrder(T->rchild);
    }
}

// 非递归中序遍历（栈）
void InOrder2(BiTree T) {
    InitStack(S); BiTree p = T;
    while (p || !StackEmpty(S)) {
        if (p) { Push(S, p); p = p->lchild; }
        else { Pop(S, p); visit(p); p = p->rchild; }
    }
}

// 层次遍历（队列）
void LevelOrder(BiTree T) {
    InitQueue(Q); EnQueue(Q, T);
    while (!QueueEmpty(Q)) {
        DeQueue(Q, p);
        visit(p);
        if (p->lchild) EnQueue(Q, p->lchild);
        if (p->rchild) EnQueue(Q, p->rchild);
    }
}
```

### 5.3 线索二叉树

**引入目的**：加快查找结点前驱和后继的速度。

```c
typedef struct ThreadNode {
    ElemType data;
    struct ThreadNode *lchild, *rchild;
    int ltag, rtag; // 0=指向孩子, 1=指向前驱/后继
} ThreadNode, *ThreadTree;
```

- **中序线索二叉树**中：
  - 第一个被访问的结点（最左下）左线索为 NULL
  - 最后一个被访问的结点（最右下）右线索为 NULL

### 5.4 树和森林

#### 树的存储结构
| 存储方式 | 结构 |
|----------|------|
| 双亲表示法 | `data + parent 下标` |
| 孩子表示法 | 邻接表形式 |
| 孩子兄弟表示法 | `data + firstchild + nextsibling`（最重要） |

#### 树、森林与二叉树的转换
- **树 → 二叉树**：左孩子右兄弟
- **森林 → 二叉树**：每棵树转二叉树，再依次连接根结点
- **二叉树 → 树/森林**：逆过程

#### 树和森林的遍历
| 树 | 森林 | 对应的二叉树 |
|----|------|:---:|
| 先根遍历 | 先序遍历 | 先序遍历 |
| 后根遍历 | 中序遍历 | 中序遍历 |

### 5.5 树的应用

#### 哈夫曼树（最优二叉树）
- **带权路径长度 WPL** = $\sum_{i=1}^{n} w_i \cdot l_i$
- WPL 最小的二叉树称为哈夫曼树
- **构造算法**：每次选两个权值最小的结点合并
- **哈夫曼编码**：左分支 0，右分支 1；前缀编码（没有一个编码是另一个的前缀）

#### 并查集
```c
int father[MaxSize];
// 初始化：每个元素自成集合
// Find：查找根结点（路径压缩优化）
// Union：合并两个集合
```

---

## 六、图

### 6.1 图的基本概念

| 术语 | 含义 |
|------|------|
| 无向图 | 边没有方向，$(v, w)$ |
| 有向图 | 边有方向，$\langle v, w \rangle$（弧） |
| 简单图 | 无重复边，无自环 |
| 完全图 | 无向完全图 $\frac{n(n-1)}{2}$ 条边；有向完全图 $n(n-1)$ 条边 |
| 连通 | 无向图中 $v$ 到 $w$ 有路径 |
| 连通图 | 任意两点都连通的无向图 |
| 连通分量 | 无向图的极大连通子图 |
| 强连通图 | 有向图中任意两点互相可达 |
| 生成树 | 包含全部顶点的极小连通子图（$n-1$ 条边） |

#### 图的基本性质
- 无向图所有顶点的度之和 = $2|E|$
- 有向图所有顶点的入度之和 = 出度之和 = $|E|$
- $n$ 个顶点的连通图至少有 $n-1$ 条边
- $n$ 个顶点的强连通图至少有 $n$ 条边（形成一个环）

### 6.2 图的存储结构

#### 邻接矩阵
```c
#define MaxVertexNum 100
typedef struct {
    VertexType vexs[MaxVertexNum];
    EdgeType edges[MaxVertexNum][MaxVertexNum];
    int vexnum, arcnum;
} MGraph;
```
- **空间复杂度**：$O(n^2)$
- **适合稠密图**
- **无向图的邻接矩阵是对称矩阵**

#### 邻接表
```c
typedef struct ArcNode {
    int adjvex;
    struct ArcNode *nextarc;
    // InfoType info; // 权值
} ArcNode;

typedef struct VNode {
    VertexType data;
    ArcNode *firstarc;
} VNode, AdjList[MaxVertexNum];

typedef struct {
    AdjList vertices;
    int vexnum, arcnum;
} ALGraph;
```
- **空间复杂度**：无向图 $O(|V| + 2|E|)$，有向图 $O(|V| + |E|)$
- **适合稀疏图**

#### 十字链表（有向图）
- 同时存储出边和入边信息

#### 邻接多重表（无向图）
- 每条边只用一个结点表示，方便边的删除操作

### 6.3 图的遍历

#### 广度优先搜索（BFS）
- 类似树的层次遍历，用队列实现
- **时间复杂度**：邻接矩阵 $O(n^2)$，邻接表 $O(n+e)$
- **空间复杂度**：$O(n)$
- 可求**无权图最短路径**

#### 深度优先搜索（DFS）
- 类似树的先序遍历，用递归/栈实现
- **时间复杂度**：邻接矩阵 $O(n^2)$，邻接表 $O(n+e)$
- **空间复杂度**：$O(n)$（递归深度）

**BFS/DFS 生成的边分类**：
| 遍历方式 | 生成树边 | 回边 |
|----------|----------|------|
| DFS（无向图） | 树边 | 回边 |
| BFS（无向图） | 树边 | 横叉边 |

### 6.4 图的应用

#### 最小生成树（MST）

| 算法 | 思想 | 时间复杂度 | 适合 |
|------|------|:---:|------|
| **Prim** | 每次选连接当前集合的最小权边 | $O(n^2)$ | 稠密图 |
| **Kruskal** | 每次选不构成回路的最小权边 | $O(e\log e)$ | 稀疏图 |

#### 最短路径

| 算法 | 适用 | 时间复杂度 |
|------|------|:---:|
| **Dijkstra** | 单源、非负权 | $O(n^2)$ |
| **Floyd** | 所有顶点对 | $O(n^3)$ |

**Dijkstra 算法**：基于贪心策略，每次选当前距离最小的顶点加入已确定集合。
**Floyd 算法**：动态规划，`dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])`。

#### 拓扑排序
- **AOV 网**：顶点表示活动，边表示先后关系
- 拓扑排序判断有向图是否有环（存在环则不能拓扑排序）
- 实现：每次找一个入度为 0 的顶点输出并删除

#### 关键路径
- **AOE 网**：边表示活动，顶点表示事件，边权表示活动持续时间
- **关键路径**：从源点到汇点最长的路径，其长度即整个工程的最短完成时间

| 量 | 含义 | 计算 |
|----|------|------|
| $ve(k)$ | 事件最早发生时间 | 从前往后取 **max** |
| $vl(k)$ | 事件最迟发生时间 | 从后往前取 **min** |
| $e(i)$ | 活动最早开始时间 | 弧尾 $ve$ |
| $l(i)$ | 活动最迟开始时间 | 弧头 $vl$ - 活动时间 |
| $d(i)$ | 时间余量 | $l(i) - e(i)$ |

- $d(i) = 0$ 的活动为**关键活动**，构成关键路径

---

## 七、查找

### 7.1 查找的基本概念
- **查找长度**：关键字比较次数
- **平均查找长度（ASL）**：$\text{ASL} = \sum_{i=1}^{n} P_i C_i$
  - $P_i$：第 $i$ 个记录被查找的概率
  - $C_i$：找到第 $i$ 个记录所需的比较次数

### 7.2 顺序查找
- **ASL 成功**：$\frac{n+1}{2}$
- **ASL 失败**：$n+1$（有哨兵时 $n+1$）
- 对表结构无要求

### 7.3 折半查找（二分查找）
- **前提**：有序的顺序表
- **时间复杂度**：$O(\log_2 n)$
- **判定树**：平衡二叉树
- **ASL**：$\approx \log_2(n+1) - 1$

```c
int BinarySearch(ElemType A[], ElemType key, int n) {
    int low = 0, high = n - 1, mid;
    while (low <= high) {
        mid = (low + high) / 2;
        if (A[mid] == key) return mid;
        else if (A[mid] > key) high = mid - 1;
        else low = mid + 1;
    }
    return -1;
}
```

### 7.4 分块查找（索引顺序查找）
- 块内无序，块间有序
- **时间复杂度**：$O(\sqrt{n})$（当块大小 $\approx \sqrt{n}$ 时）

### 7.5 二叉排序树（BST）

**定义**：左子树所有结点值 < 根结点值 < 右子树所有结点值（中序有序）。

**操作复杂度**：
| 操作 | 平均 | 最坏 |
|------|:---:|:---:|
| 查找 | $O(\log n)$ | $O(n)$ |
| 插入 | $O(\log n)$ | $O(n)$ |
| 删除 | $O(\log n)$ | $O(n)$ |

**删除结点三种情况**：
1. 叶结点：直接删除
2. 只有左/右子树：子树上移填补
3. 左右子树均非空：用**直接前驱**（左子树最右下）或**直接后继**（右子树最左下）替换

### 7.6 平衡二叉树（AVL）

- **平衡因子** = 左子树高度 - 右子树高度（只能为 $-1, 0, 1$）
- **最小不平衡子树**：离插入结点最近且平衡因子绝对值 > 1 的结点为根的子树

**四种调整**：

| 插入位置 | 旋转方式 |
|----------|----------|
| LL（左子树的左子树） | 右单旋 |
| RR（右子树的右子树） | 左单旋 |
| LR（左子树的右子树） | 先左旋后右旋 |
| RL（右子树的左子树） | 先右旋后左旋 |

- 含 $n$ 个结点的 AVL 树最大深度为 $O(\log n)$

### 7.7 红黑树

**五个性质**（核心考点）：
1. 每个结点要么是红色，要么是黑色
2. **根结点是黑色**
3. 叶结点（NIL）是黑色
4. 红结点的两个孩子必须是黑色（**不能有两个连续的红结点**）
5. 从任一结点到其每个叶结点的所有路径都包含相同数量的黑结点（**黑高相同**）

**结论**：
- 从根到叶的最长路径长度不超过最短路径的 2 倍
- 红黑树是"近似平衡"的二叉查找树

### 7.8 B 树与 B+ 树

#### B 树（m 阶）
**定义**：
- 每个结点至多 $m$ 棵子树，至多 $m-1$ 个关键字
- 根结点至少 2 棵子树（除非是叶结点）
- 非根非叶结点至少 $\lceil m/2 \rceil$ 棵子树，至少 $\lceil m/2 \rceil - 1$ 个关键字
- 所有叶结点在同一层（不包含信息，实际为 NULL）

**特点**：
- 平衡的多路查找树
- 所有结点的平衡因子 = 0
- 适合**外存查找**（数据库索引）

**B 树高度**：含 $n$ 个关键字的 $m$ 阶 B 树
- 最小高度：$\log_m(n+1)$
- 最大高度：$\log_{\lceil m/2 \rceil}((n+1)/2) + 1$

#### B+ 树
**与 B 树的区别**（高频考点）：
| 比较项 | B 树 | B+ 树 |
|--------|------|-------|
| 关键字分布 | 每个结点含关键字 | 只有叶结点含全部关键字 |
| 重复关键字 | 不重复 | 叶结点含全部，上层只做索引 |
| 叶结点链接 | 不链接 | 叶结点按序链接成链表 |
| 查找方式 | 可能在非叶结点命中 | 必须查到叶结点 |

### 7.9 散列表（哈希表）

- **散列函数**：将关键字映射为地址的函数
- **冲突**：不同关键字映射到同一地址

#### 常见散列函数
| 方法 | 说明 |
|------|------|
| 除留余数法 | $H(key) = key \bmod p$，$p$ 取不大于表长的素数 |
| 直接定址法 | $H(key) = a \cdot key + b$ |
| 平方取中法 | 取关键字平方的中间几位 |
| 数字分析法 | 分析关键字各位分布选取 |

#### 冲突解决方法

**开放定址法**：$H_i = (H(key) + d_i) \bmod m$
| 方法 | $d_i$ |
|------|------|
| 线性探测 | $0, 1, 2, \dots, m-1$ |
| 平方探测 | $0, 1^2, -1^2, 2^2, -2^2, \dots$ |
| 双散列 | $i \times H_2(key)$ |
| 伪随机序列 | 伪随机数 |

**拉链法**：每个散列地址对应一个链表。

#### 散列表查找效率
- 取决于：散列函数、冲突处理方法、**装填因子** $\alpha = \frac{n}{m}$

---

## 八、排序

### 8.1 排序的基本概念
- **稳定性**：若 $key_i = key_j$ 且排序前 $i$ 在 $j$ 前，排序后 $i$ 仍在 $j$ 前，则算法稳定
- **内部排序**：数据全部在内存中
- **外部排序**：数据量太大，需内外存交换

### 8.2 插入排序

#### 直接插入排序
- **思想**：将记录插入已排好序的序列中
- **时间复杂度**：最好 $O(n)$，最坏 $O(n^2)$，平均 $O(n^2)$
- **空间复杂度**：$O(1)$
- **稳定** ✅

#### 折半插入排序
- 查找插入位置用折半查找（减少比较次数）
- **时间复杂度**：$O(n^2)$（移动次数不变）
- **稳定** ✅

#### 希尔排序
- 将序列按增量分组，组内直接插入排序，增量逐步减小
- **时间复杂度**：约 $O(n^{1.3})$，最坏 $O(n^2)$
- **空间复杂度**：$O(1)$
- **不稳定** ❌

### 8.3 交换排序

#### 冒泡排序
- **时间复杂度**：最好 $O(n)$，最坏 $O(n^2)$
- **空间复杂度**：$O(1)$
- **稳定** ✅

#### 快速排序
- **思想**：选枢轴，一趟划分后左边 ≤ 枢轴 ≤ 右边，递归处理
- **时间复杂度**：最好/平均 $O(n \log n)$，最坏 $O(n^2)$（有序时）
- **空间复杂度**：$O(\log n)$（递归栈）
- **不稳定** ❌
- **优化**：三数取中法选枢轴、小规模时用插入排序

### 8.4 选择排序

#### 简单选择排序
- 每趟选最小元素与第 $i$ 个位置交换
- **时间复杂度**：始终 $O(n^2)$
- **空间复杂度**：$O(1)$
- **不稳定** ❌（交换可能破坏顺序）

#### 堆排序
- **堆的定义**：大根堆（$key_i \ge key_{2i}$ 且 $key_i \ge key_{2i+1}$），小根堆反之
- **时间复杂度**：始终 $O(n \log n)$
- **空间复杂度**：$O(1)$
- **不稳定** ❌

**建堆**：从 $\lfloor n/2 \rfloor$ 开始向前调整，$O(n)$
**堆排序**：依次取堆顶（与末尾交换），再调整，$O(n \log n)$

```c
// 大根堆向下调整
void HeapAdjust(int A[], int k, int len) {
    A[0] = A[k]; // A[0] 暂存
    for (int i = 2*k; i <= len; i *= 2) {
        if (i < len && A[i] < A[i+1]) i++; // 找较大孩子
        if (A[0] >= A[i]) break;
        A[k] = A[i]; k = i;
    }
    A[k] = A[0];
}
```

### 8.5 归并排序（二路归并）

- **思想**：将两个有序表合并为一个有序表，递归分治
- **时间复杂度**：始终 $O(n \log n)$
- **空间复杂度**：$O(n)$
- **稳定** ✅

### 8.6 基数排序

- 按关键字的各位（个位→十位→百位）进行分配和收集
- **时间复杂度**：$O(d(n + r))$，$d$ 为位数，$r$ 为基数
- **空间复杂度**：$O(r)$
- **稳定** ✅

### 8.7 各种排序算法比较（必背！）

| 排序算法 | 最好时间 | 平均时间 | 最坏时间 | 空间 | 稳定性 |
|----------|:---:|:---:|:---:|:---:|:---:|
| 直接插入排序 | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 |
| 希尔排序 | — | $O(n^{1.3})$ | $O(n^2)$ | $O(1)$ | ❌ |
| 冒泡排序 | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 |
| 快速排序 | $O(n\log n)$ | $O(n\log n)$ | $O(n^2)$ | $O(\log n)$ | ❌ |
| 简单选择排序 | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ |
| 堆排序 | $O(n\log n)$ | $O(n\log n)$ | $O(n\log n)$ | $O(1)$ | ❌ |
| 二路归并排序 | $O(n\log n)$ | $O(n\log n)$ | $O(n\log n)$ | $O(n)$ | ✅ 稳定 |
| 基数排序 | $O(d(n+r))$ | $O(d(n+r))$ | $O(d(n+r))$ | $O(r)$ | ✅ 稳定 |

### 8.8 排序算法选择策略

| 场景 | 推荐算法 |
|------|----------|
| $n$ 较小（≤ 50） | 直接插入排序或简单选择排序 |
| 基本有序 | 直接插入排序或冒泡排序 |
| $n$ 较大，要求稳定 | 归并排序 |
| $n$ 较大，关键字随机 | 快速排序（平均最优） |
| $n$ 较大，关键字有规律 | 堆排序 $O(n\log n)$ 且不递归 |
| $n$ 很大，关键字位数少 | 基数排序 |

---

## 九、常见算法设计题考点

### 9.1 常考题型
1. **链表操作**：逆置、合并有序链表、找倒数第 k 个结点、判断环
2. **二叉树操作**：求高度、宽度、遍历的非递归实现、判断是否完全二叉树
3. **图算法**：DFS/BFS 的应用、拓扑排序
4. **查找应用**：散列表设计、二叉排序树操作判断
5. **排序综合**：各种排序的模拟过程

### 9.2 链表高频算法

```c
// 1. 单链表逆置（三指针法）
LinkList Reverse(LinkList L) {
    LNode *pre = NULL, *cur = L->next, *next;
    while (cur) {
        next = cur->next;
        cur->next = pre;
        pre = cur;
        cur = next;
    }
    L->next = pre;
    return L;
}

// 2. 找倒数第 k 个结点（双指针）
LNode* FindKthToTail(LinkList L, int k) {
    LNode *fast = L->next, *slow = L->next;
    while (k-- && fast) fast = fast->next;
    while (fast) { slow = slow->next; fast = fast->next; }
    return slow;
}

// 3. 判断链表是否有环（快慢指针）
bool HasCycle(LinkList L) {
    LNode *slow = L, *fast = L;
    while (fast && fast->next) {
        slow = slow->next;
        fast = fast->next->next;
        if (slow == fast) return true;
    }
    return false;
}
```

### 9.3 二叉树高频算法

```c
// 1. 求二叉树高度（后序遍历）
int GetHeight(BiTree T) {
    if (!T) return 0;
    int lh = GetHeight(T->lchild);
    int rh = GetHeight(T->rchild);
    return (lh > rh ? lh : rh) + 1;
}

// 2. 判断是否完全二叉树（层次遍历）
bool IsComplete(BiTree T) {
    if (!T) return true;
    InitQueue(Q); EnQueue(Q, T);
    bool mustBeLeaf = false;
    while (!QueueEmpty(Q)) {
        DeQueue(Q, p);
        if (mustBeLeaf && (p->lchild || p->rchild)) return false;
        if (p->lchild) EnQueue(Q, p->lchild);
        else if (p->rchild) return false;
        if (!p->rchild) mustBeLeaf = true;
        else EnQueue(Q, p->rchild);
    }
    return true;
}
```

---

## 十、考前速记口诀

| 口诀 | 内容 |
|------|------|
| **"删插移一半"** | 顺序表插入删除需移动约 $n/2$ 个元素 |
| **"二叉性质三"** | $n_0 = n_2 + 1$ |
| **"哈夫曼选最小"** | 每次合并最小的两个 |
| **"普里姆选最近"** | Prim 选离当前集合最近的顶点 |
| **"克鲁斯卡尔选最小边"** | Kruskal 选最小且不形成回路的边 |
| **"堆排建堆从半起"** | 从 $\lfloor n/2 \rfloor$ 开始调整 |
| **"快排枢轴定位置"** | 每趟确定枢轴元素的最终位置 |
| **"归并二合一用 O(n) 空间"** | 归并排序需要 $O(n)$ 辅助空间 |
| **"AVL 四种旋转记 LL/RR"** | LL右旋、RR左旋、LR左右、RL右左 |
| **"红黑五性：根黑叶黑不连红，黑高相同"** | 红黑树五条性质 |

---

> 📚 **参考资料**：严蔚敏《数据结构（C语言版）》、王道考研系列
